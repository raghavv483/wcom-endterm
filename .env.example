# Supabase (required for production)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# When using the Supabase Edge Function `fetch-youtube-videos`, the function
# needs a YouTube Data API key set in the function's environment (YOUTUBE_API_KEY).
# For local development, you can optionally force the app to use built-in demo
# videos instead of calling the remote function by setting:
VITE_USE_MOCK_VIDEOS=false

# Optional: if running with YT API keys in a dev environment, keep them secure
# Do NOT commit secret keys to your repository. Use environment configuration
# provided by your hosting (Vercel, Netlify, Supabase, etc) for production.

# GROQ API (optional)
# URL of your GROQ AI endpoint to POST chat messages to. If not set, the UI will
# show a friendly fallback message instead of calling the remote API.
VITE_GROQ_API_URL=https://api.groq.com/openai/v1/chat/completions
# Optional API key for the GROQ endpoint (if required)
VITE_GROQ_API_KEY=your-groq-api-key-here
